services:
  disco-bot:
    build: .
    container_name: disco_ai_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ./users.md:/app/users.md
      - ./prompt.txt:/app/prompt.txt
    network_mode: host
